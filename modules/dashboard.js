angular.module('dashboard-module',['bootstrap-growl','bootstrap-modal','form-validator']).factory('dashboard', function($http,$timeout,$compile,growl,validate,bootstrapModal){

	function dashboard(){			// The main function of your controller.

		var self = this;

		
	return new dashboard();		// Returns new value of a class.
});